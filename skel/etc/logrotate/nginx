/var/log/nginx/*.log {
	rotate 28
	daily
	compress
	delaycompress
	missingok
	notifempty
	create 640 nginx adm
	sharedscripts
	postrotate
		if [ -f /run/nginx.pid ]; then
			kill -USR1 `cat /run/nginx.pid`
		fi
	endscript
}
